// The Requests dataset, so we can capture the request path, the number (1), the request speed and the timetstamp
{
  "fields": {
    "path": {
      "type": "string",
      "name": "Request Path"
    },
    "count": {
      "type": "number",
      "name": "Count"
    },
    "speed": {
      "type": "number",
      "name": "Request Speed"
    },
    "timestamp": {
      "type": "datetime",
      "name": "Date"
    }
  }
}